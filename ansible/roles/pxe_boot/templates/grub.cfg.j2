set timeout=0
set default=0

menuentry "Ubuntu 22.04 NFS Boot" {
    linux /boot/grub/vmlinuz root=/dev/nfs nfsroot={{ hostvars['localhost']['ansible_' + compute_interface]['ipv4']['address'] }}:/srv/nfs/ubuntu2204 ip=dhcp quiet splash overlayroot=tmpfs
    initrd /boot/grub/initrd
}
