---
- name: Run venv root tasks
  ansible.builtin.include_tasks:
    file: root.yml
    apply:
      become: true

- name: Run venv user tasks
  ansible.builtin.include_tasks:
    file: user.yml
    apply:
      become: true
      become_user: compute
